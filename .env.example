# Server configuration
PORT=3000

# Optional: set to 'false' to disable SMTP checks globally (faster but less accurate)
# Default: true
# SMTP_CHECK=true

# Optional: enable debug SMTP logs in responses
# Default: false
# DEBUG=false
